--Simple premake5 config for initializing a visual studio 2022 project and SDL2

--In order to build the project open a windows terminal in the project's root
  folder and type "./build.bat compile" without the quotation marks.